# Dear imgui CMake project template

This is a template to be used for CMake-based imgui projects.

## Building
```
$ mkdir build
$ cd build
$ cmake ..
$ make -j$(nproc)
```

The resulting file will be at *../bin*.
